/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "github.com",
      "images.unsplash.com",
      "nextuipro.nyc3.cdn.digitaloceanspaces.com",
    ],
  },
}

export default nextConfig
